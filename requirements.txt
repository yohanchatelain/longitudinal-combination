numpy
pandas
nibabel
scipy
scikit-learn
# Pinned to a CUDA 12.1 build: the cluster's GPU node driver (545.23.08) tops
# out at CUDA 12.3, and an unpinned `torch` resolves to a cu13x build that
# silently reports torch.cuda.is_available() == False on this cluster.
# Install with: uv pip install --index-url https://download.pytorch.org/whl/cu121 torch==2.5.1
torch==2.5.1+cu121
PyYAML
tqdm
statsmodels
matplotlib
seaborn
shap
